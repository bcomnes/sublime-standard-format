{
    // use either "standard" OR "semistandard" for .js .jsx files
    "standard_format": "standard",
    // whether to enable support for ts-standard and .ts .tsx files
    "enable_typescript": false,
    // toggle on/off formatting the current file via the Command Palette
    "format_on_save": true,
    // this package uses $PATH environment variable,
    // you can set an arbitrary $PATH to override the default behavior.
    "path": [],
}